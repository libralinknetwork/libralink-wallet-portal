{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}
